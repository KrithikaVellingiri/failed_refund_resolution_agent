import React from 'react';
import styles from './SyntheticLabel.module.css';

export const SyntheticLabel: React.FC = () => {
  return (
    <span className={styles.label}>
      SYNTHETIC
    </span>
  );
};
